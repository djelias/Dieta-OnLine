<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pdm115.sv.fia.ues.dietaonline">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegistrarUsuario" />
        <activity android:name=".IniciarSesion" />
        <activity android:name=".PerfilSalud" />
        <activity android:name=".CrearDieta" />
        <activity android:name=".CrearMenu" />
        <activity android:name=".GestionarEstadisticas" />
        <activity android:name=".GestionarResultados" />
        <activity android:name=".MenuAdministrador" />
        <activity android:name=".CrearEstadistica" />
        <activity android:name=".IngresarResultados"></activity>
    </application>

</manifest>